<%= link_to 'トップページへ戻る',root_path %>
<h2>クイズ作成</h2>

<%= form_with model: @quiz, url:quizzes_path ,local: true do |f| %>
  <%= render 'shared/error_messages', model: f.object %>

  <%= f.text_area :quiz_name, placeholder:"クイズ名（必須です 40文字まで)", maxlength:"40" %><br>
  <p>ジャンル・難易度</p>
  <%= f.collection_select(:quiz_category_id, QuizCategory.all, :id, :date, {}, {class:"hoge", id:"hoge"}) %>
  <%= f.collection_select(:quiz_level_id, QuizLevel.all, :id, :date, {}, {class:"hoge", id:"hoge"}) %><br>
  <p>難易度のイメージとして</p>
  <p>IT関連の用語を使わずに説明できるものが「初心」</p>
  <p>「初心」の用語のみで説明できるものが「初級」</p>
  <p>「初級」の用語が理解できてなければ説明できないものが「中級」</p>
  <p>迷ったらこのような感覚で決定してもらえると良いかもしれません</p>
  <%= f.text_area :quiz_text, placeholder:"問題文（入力必須です)"%><br>
  <%= f.text_area :quiz_answer, placeholder:"解答（入力必須です)"%><br>
  <%= f.text_area :quiz_sample, placeholder:"解説・例文・使用例（入力は任意です)"%>
  <%= f.submit "出題する"%>
<% end %>